# 把題庫藏起來：repo 轉 private 的完整程序

> 查證日期 2026-09-24。做之前先把「第 0 步」的岔路想清楚，那決定了後面要走哪一條。

## 現況（我實際抓過，不是推測）

| 東西 | 位置 | 現在誰看得到 |
|---|---|---|
| 題庫原始檔 `questions/*.json` | public repo | **任何人**。`raw.githubusercontent.com/fctien/Quiz_Game/main/questions/ai.json` 直接回 HTTP 200 |
| 課程題目 | 同上，共 2,178 題（python 500、pytorch 738、ai 510、vba 280、emba 150） | **任何人**，含答案與解析 |
| 公開單機版 | `https://fctien.github.io/Quiz_Game/` | 任何人。這一頁裡**明碼嵌著 1,742 題**（人文 463、地理 391、歷史 388、科技 125、娛樂 125、趣聞 125、桌球 125） |
| 執行中的網站 | Render | 任何人。`/api/categories`、`/api/start` 會直接發題目出去 |
| `course_code.txt` | public repo | **任何人**。它在 `.gitignore` 裡，但 `.gitignore` 只管還沒被追蹤的檔案——這個檔在被加進去之前就 commit 過了 |

重點：**repo 轉 private 只解決上表第 1、2、5 列。** 第 3、4 列是另外兩件事，要分開處理。

---

## 第 0 步：先決定公開單機版的去留（這是岔路）

`docs/index.html` 本身就嵌著 1,742 題明碼。所以「所有題庫都不公開」和「保留現在的公開單機版」是互相矛盾的，只能三選一：

**甲案 — 公開頁不要了。** 整個 repo 轉 private，Pages 跟著關掉。最乾淨，步驟最少（做第 1～4、6、7 步，跳過第 5 步）。代價是失去一個不需要開伺服器的備援網址。

**乙案 — 公開頁留著，接受那 1,742 題是公開的。** 另開一個 public repo 只放那一頁。課程 2,178 題完全不公開，通識與輕鬆的 1,742 題維持公開。做全部七步。

**丙案 — 公開頁留著，但不放任何題目。** 那一頁改成只有備援說明與課堂小抄，沒有題庫。Pages 保留，但它就不是一個能玩的單機版了。做全部七步，第 5 步換成放不含題目的頁。

GitHub Free 方案**不支援**從 private repo 發佈 Pages（要 GitHub Pro 才行，個人帳號），所以沒有「repo 是 private、Pages 還照常從 docs/ 出」這個選項。你要保持不付費，就只能甲／乙／丙擇一。

---

## 第 1 步：備份

把整個 `quiz_game` 資料夾複製成 `_備份_20260924`（你已經有 `_備份_20260918` 的習慣）。
下面每一步都可逆，但轉 visibility 那一步 GitHub 會要你打字確認，先備份心安。

## 第 2 步：repo 轉 private

1. 開 `https://github.com/fctien/Quiz_Game`
2. **Settings** → 拉到最下面 **Danger Zone**
3. **Change repository visibility** → **Make private** → 照它的要求打一次 repo 名稱 → 確認

轉完之後會發生的事：

- raw 網址對未登入的人立刻變 404（題庫外人抓不到了）
- **GitHub Pages 會停掉**，`fctien.github.io/Quiz_Game/` 變 404
- 已經存在的 public fork 會被 GitHub 切斷（detach），不會跟著變 private
- **別人早就 clone 或下載走的副本不會消失**。這一步是止血，不是回收

## 第 3 步：確認 Render 還連得上 private repo

轉 private 之後 Render 需要有權限才 clone 得到。

1. GitHub → 右上頭像 → **Settings** → **Applications** → **Installed GitHub Apps** → **Render** → **Configure**
2. 看 **Repository access**：如果是 *Only select repositories*，確認 `Quiz_Game` 在清單裡；不在就加進去 → Save
3. 回 Render → 你的 service → **Manual Deploy** → **Deploy latest commit**
4. 看 build log 開頭有沒有 clone 成功

**我不確定的地方**：Render 官方的免費方案說明裡列了一堆限制（15 分鐘沒流量就休眠、750 小時、沒有持久磁碟），但**沒有提到 private repo 這一條限制**。我沒有實際在免費方案上用 private repo 部署過，所以不敢跟你打包票。這一步如果卡住，先不要慌，回來告訴我 build log 寫什麼。

建議把第 2 步和第 3 步排在同一個時段做，萬一 Render 真的不吃，可以馬上把 repo 轉回 public。

## 第 4 步：處理已經漏出去的東西

**4-1 `course_code.txt`**

```bash
git rm --cached course_code.txt
git commit -m "課程代碼不進版控"
git push
```

`--cached` 是只從版控移除、電腦上的檔案留著。但要知道：**舊的 commit 裡還有這個檔**。repo 轉 private 之後外人看不到歷史，所以不必動用 `git filter-repo` 去改寫歷史（那會讓所有 commit hash 變掉，Render 要重指，不值得）。

不過那組代碼已經公開躺了一段時間，**直接換一組新的比較實在**。

**4-2 `班級管理碼.txt` 還沒進 `.gitignore`**

`classroom.py` 會把每個班級的管理碼寫進 `班級管理碼.txt`。這個檔現在不在 `.gitignore` 裡，將來一旦產生就會被 `git add -A` 一起推上去。我已經幫你補進去了，確認一下：

```
leaderboard.db
__pycache__/
*.pyc
questions/_drafts/
.venv/
course_code.txt
班級管理碼.txt
```

## 第 5 步：公開頁（甲案跳過）

**乙案（公開頁照舊，含 1,742 題）**

1. GitHub 開一個新的 **public** repo，例如 `fctien/pulse`
2. 把 `docs/index.html` 複製過去，改名 `index.html`，推上去
3. 新 repo → **Settings** → **Pages** → Source 選 **Deploy from a branch** → `main` / `/ (root)` → Save
4. 新網址變成 `https://fctien.github.io/pulse/`
5. **印好的 QR 和小抄要換網址**

**丙案（公開頁不含題目）**

同上，但放的是不含題庫的頁（備援說明、課堂小抄）。這頁要我另外做。

兩案都要順手改 `build_standalone.py`：現在它把公開版寫到 `docs/index.html`，之後要多一個「複製到新 repo 再推」的步驟。我可以寫一個 `發佈公開頁.bat` 把這件事包起來。

## 第 6 步：把站台本身鎖上（**已完成，2026-09-24**）

repo 變 private 了，但 Render 上跑的網站還是公開的。任何人拿到你的 Render 網址，`POST /api/start {"category":"ai"}` 一樣拿得到 AI 導論的題目。所以要在伺服器端擋。

做法沿用你 `classroom.py` 裡已經有的 `MASTER_CODE` 那一套（密碼放環境變數，不進檔案）：

- 環境變數 `TEACH_CODE`：Render 後台 **Environment** → Add Environment Variable；本機用 `啟動-Windows.bat` 裡 `set TEACH_CODE=...`（這個 .bat 要記得別把密碼寫死後推上去——或者放在一個 gitignore 的 `本機設定.bat` 裡）
- 新增 `POST /api/unlock`：比對 `TEACH_CODE`，對了就發一個簽名 cookie，存 12 小時
- 未解鎖時：`/api/categories` 不列課程主題、`/api/units` 對課程主題回空陣列、`/api/start` 與 `/api/mp/create` 對課程主題回 403
- 主持台上「課程」那一頁預設不出現，按「解鎖課程題庫」輸入密碼後才出現

**這一段已經做好了**（v6.2），實際落地的樣子跟上面的規劃有兩處不同：

- 新增 `gate.py` 集中處理門禁，端點只呼叫 `gate.allow(topic, request)`，不各自寫判斷。
- **速率限制拿掉了。** 本來要在 `/api/start` 和 `/api/mp/create` 加「同一 IP 每分鐘 N 次」，
  實測時發現這會誤傷：整班學生在同一個校園 NAT 後面，伺服器看到的是同一個 IP，
  55 個人同時開局就是 55 次請求，任何合理門檻都會把整班擋在外面。
  真正的防線是 cookie 門禁（擋的是身分，不是次數），那個不會誤傷任何人。
  速率限制只留在「試解鎖碼」那裡，防人硬猜密碼。

行為摘要：

| 情況 | 課程主題 |
|---|---|
| 本機、沒設 `TEACH_CODE` | 直接開放，跟以前一模一樣 |
| 有設 `TEACH_CODE` | 要先解鎖（主持台上輸入，12 小時有效，綁這台瀏覽器） |
| 雲端、忘了設 `TEACH_CODE` | 整批關閉，而且解不開（fail-safe） |

實測數字：改之前，未登入的人 **1 秒內**就從 `/api/start` + `/api/answer` 撈到
**284 個 AI 導論題幹和 162 個正解**；改之後同一支腳本是 **0 和 0**。

## 第 7 步：驗收（每一項都要自己跑一遍）

1. 開**無痕視窗**（確保沒登入 GitHub），連 `https://raw.githubusercontent.com/fctien/Quiz_Game/main/questions/ai.json` → 應該是 **404**
2. 無痕連 `https://github.com/fctien/Quiz_Game` → 應該是 **404**（private repo 對外人就是不存在）
3. 無痕連 `https://fctien.github.io/Quiz_Game/` → 甲案應該 404；乙丙案改連新網址應該正常
4. 無痕連你的 Render 網址 → 課程那一頁看不到
5. 無痕對 Render 發 `POST /api/start`，body `{"category":"ai","count":5}` → 應該 **403**
6. 你自己輸入 `TEACH_CODE` → 課程分頁出現，能開考坊、能出題
7. Render 重新部署一次，確認 build 沒壞

---

## 建議的順序

如果照我的判斷排，會是這樣：

1. ~~**先做第 6 步（站台上鎖）+ 主題分類分頁**~~ —— **已完成（v6.2）**。你只要到 Render 後台設一組 `TEACH_CODE`，這一步就生效了
2. **再做第 4 步** —— 換掉課程代碼、補 `.gitignore`，五分鐘的事
3. **最後做第 2、3 步（轉 private）** —— 挑一個你有空盯著 Render 的時段做，萬一不吃可以馬上轉回來
4. **第 5 步看你第 0 步怎麼選**

反過來先轉 private 也行，只是那樣中間會有一段時間「repo 藏起來了，但網站還在發題目」，防護是假的。
