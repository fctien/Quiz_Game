# 脈衝 PULSE 開發歷程

只追加，不改寫。欄位：需求／做法／產出／驗證／問題與修正／待辦。
本檔自 2026-09-18 起記錄；在此之前的歷程請看 `git log`。

---

## 2026-09-18　v6.0　地理／歷史／人文改成分地區出題

**需求**
使用者：「在地理、歷史、人文等部分，分區域：如中國、台灣、歐洲、美洲、中南美洲等，
規劃一下如何區分比較好，讓 host 可以選擇區域出題，再分別增加區域來出題，
例如：中國地理可加入河北省會是哪裡？中國分多少省？最大的省？等等」，
並要求再做一次 60 人壓力測試。
確認過的選項：分 7 區、每區每主題補到 55 題、主持台「先選主題再選地區」。

**做法**
- 地區不另做一套機制，直接沿用 Python 週次那套 `unit`，主持台與單機版的既有 UI 不必改。
- 七區：TW 台灣／CN 中國／EA 日韓・東亞／SEA 東南亞・南亞／EU 歐洲／AM 美洲／
  OTH 中東・非洲・大洋洲；人文另保留 POEM 古典詩詞。
- `app.py` 新增 `REGION_ORDER`、`REGION_TOPICS`，`units_of()` 依此排序，
  並濾掉題數少於 5 題的單元；`build_standalone.py` 做同樣排序，離線版順序才會一致。
- 新題庫放在 `questions/r_*.json`，一個地區一個檔。
- 出題：21 個（主題 × 地區）各派一個 agent 撰寫並自行查證，共補 860 題。
- 查核：另外派 11 個 agent，拿到的是「只有題目、沒有出處」的檔案，逐題獨立覆核。

**產出**
- 新增 860 題，21 個組合各達 55 題；題庫 2370 → 3230 題。
- `questions/r_taiwan / r_china / r_eastasia / r_southasia / r_europe / r_america / r_other.json`
- 版本 v5.9 → v6.0（`static/*.html`、`DEPLOY-Render.md`、`docs/*.html` 一併更新）
- `docs/課堂小抄.html` 補上「單元／地區」欄位說明

**驗證**
- 21 個（主題 × 地區）各抽三輪、共 1260 題：伺服器發的題目、選項、正解、判分
  與題庫完全一致，0 件不符。
- 60 人同場：加入、送題、搶答、放榜 60/60，0 錯誤，送題平均 15 ms。
- 60 人考到一半中斷 → 換一套題目 → 重新開始：60/60 人在 0.3 秒內跟上。
- 三位老師同時開課 165 人：送題 165/165，0 錯誤，記憶體 66 MB。
- CPU 每題 0.20 秒（換算 Render 免費方案約 2.0 秒）。
- 主持台、單機版、公開版三邊 UI 皆正常，無 JS 錯誤。

**問題與修正**
- 860 題查出 37 個問題，**沒有一題答案錯**，全部是解說用字、譯名一致性或題幹精確度。
  例：「歐洲最大的活火山」是不嚴謹的最高級說法（維基只稱 one of the tallest）；
  「蒙古西部與哈薩克交界」是錯的前提（兩國並不相鄰，最近處相距約 37 公里）；
  巴拿馬運河人工湖那題有兩個選項都說得通（米拉弗洛雷斯湖也是運河上的人工湖）；
  「北条氏」用到日文新字體，應作「北條氏」。全部逐條修正。
- 錫克教創始人一題在 history_SEA 與 human_SEA 重複出現，把歷史那題換成錫克帝國的建立者。
- **失敗的嘗試**：一開始想讓判分測試一次考完整區 55 題，但 `COUNTS` 只允許 5/10/15/20，
  建房直接被擋下來；改成每區跑三輪 20 題，三輪下來每區都能抽到 36–46 種不同題目。
- **失敗的嘗試**：第一版測試腳本用 `res["correct"]` 判斷答對與否，但 `/api/mp/answer`
  回傳的是 `ok`；1260 題全被誤判成「判分不符」。改看 reveal 的 `answer` 與 `result.right`。
- 兩個 agent（human_SEA、human_AM）中途因 session rate limit 或內容過濾被中斷；
  human_AM 的檔案其實已寫完可用，human_SEA 重跑一次，內容過濾那批改成分成兩小批就通過。
- 課堂小抄加了地區說明後超出一張 A4（1013 → 1111 px），把主題列與名單 CSV 列壓成單行後回到 1039 px。

**待辦**
- 使用者自行執行 `推上GitHub.bat` 推上 GitHub，Render 重新部署後首頁應顯示 v6.0。
- GLOBAL（全球・跨區）地理僅 6 題、歷史與人文各 3 題（歷史、人文的已被自動隱藏），
  日後若要保留這個地區要再補題。

---

## 2026-09-21　Deep Learning 新增 L0-ANN 單元（類神經網路之簡介 30 題）

**需求**
使用者上傳《Lab 0 - 神經網路之簡介_2026_v1.pptx》（80 張投影片），要求
「add L0 - ANN簡介 problem bank into this game，30 problems」，隔天上課要用。
確認過的選項：新開一個單元（不動原本的 L0 環境題）、難度照慣例 25/50/25。

**做法**
- 先用 python-pptx 把 80 張投影片的文字與表格全部抽出來（含備註），作為唯一出處。
- 新單元 `L0-ANN`、`unit_name` 為「Lab0 神經網路簡介」，id 前綴 `pt-L0A-`，
  插在 `questions/pytorch.json` 最前面，主持台清單上會排在「Lab0 快速上手與環境」之前。
- 30 題平均分布在簡報的 Part 1–10：基本概念 3、發展歷史 8、分類 3、學習法則 2、
  Perceptron 4、Backpropagation 4、現代架構 4、傳統 vs 深度 1、現代挑戰 1。
- 寫完後派一個獨立查核 agent，拿題目與簡報全文逐題比對，並對外部公開資料查證。

**產出**
- `questions/pytorch.json` 708 → 738 題；題庫總數 3230 → 3260 題。
- `/mnt/user-data/outputs/PULSE-Lab0-ANN簡介-30題對照表.pdf`（列印用，綠色為正解，附解說）

**驗證**
- 結構：30 題、難度 8/15/7、正解索引 8/8/7/7、「最長的選項就是答案」13%（以顯示寬度計）。
- 判分：`L0-ANN` 單元連考四輪共 80 題，抽到 29/30 種不同題目，選項、正解、判分 0 件不符。
- UI：主持台 Deep Learning 的單元清單第一個即為「L0-ANN Lab0 神經網路簡介（30）」，
  建房標題列正確；單機版重建後同樣正確；無 JS 錯誤。
- 查核 agent 順手驗算了簡報自己的兩組手算，數字都自洽：
  AND 閘（W=[1,2]、b=0、η=1）確實在第 1 個 epoch 兩次修正後得 W=[1,1]、b=−2；
  2-2-1 BP 範例的誤差由 0.028473 降到 0.026776（−5.96%）算得出來。

**問題與修正**
- 查核查出 8 題有問題，**沒有一題答案錯**，都是解說或題幹，已全部修正：
  「Neurons that fire together, wire together」不是 Hebb 原文，是 Carla Shatz（1992）的改寫；
  AlexNet 官方 top-5 錯誤率是 15.3%（簡報寫 16%），解說補註實際數字；
  「隱藏層梯度小 100 倍」不能全歸因於 Sigmoid，有一半來自輸入值本身很小（x = 0.05、0.10）；
  BP 並非 1986 年首創（Linnainmaa 1970、Werbos 1974），1986 是普及年；
  第二次寒冬 1995–2006 是就類神經網路而言（學界通稱的第二次 AI 寒冬是 1987–1993），題幹加了限定；
  ResNet 那題的誘答「3.6%」與題幹已給的 3.57% 幾乎同值，換成 1.2%；
  pt-L0A-04 與 05 的解說都自稱「起點」，改成區分「第一個 ANN」與「第一個可學習的 ANN」。
- **沒有照單全收的一項**：查核建議把簡報投影片 7 的「Reference」改成「Reasoning」
  （認為是誤植）。這是老師自己的教材用語，不應由我們逕行更改，
  因此選項保留原文 Reference、只拿掉原本錯誤的中譯「推論」，另外向使用者提出。
- **失敗的嘗試**：第一版先寫好題目再用程式重排選項來平衡正解索引，
  結果「最長的選項就是答案」高達 43% —— 因為正解常常是完整敘述、誘答卻很短，
  單靠重排解決不了。改成把誘答逐一改寫到和正解差不多長，才降到 13%。
  另外長度改用「中文字算 1、英數算 0.5」的顯示寬度計算，比單純算字元數接近螢幕實況。
- 第一版寫了 32 題，砍掉兩題（突觸神經傳導物質、學習訊號 r 的定義，後者與另外兩題重複）
  並把「BP 依據鏈鎖律」調為簡單題，才湊成 30 題且難度剛好 8/15/7。

**待辦**
- 請使用者確認簡報投影片 7 的「Reference」是否為「Reasoning」之誤植；若要改，題目也要跟著改。
- 使用者自行執行 `推上GitHub.bat`。

---

## 2026-09-21　L0-ANN 第 3 題改用 Reasoning

**需求**
承上則待辦。使用者確認簡報投影片 7 的「Reference」應為「Reasoning」，指示「改成 Reasoning」。

**做法**
- `pt-L0A-03` 的選項與解說把 Reference 改成「推理 Reasoning」。
- 簡報本身也一起改，但依簡報最後一張的提醒**另存新檔**，不覆蓋原始檔：
  用 python-pptx 複製一份後只改該處的 run，其餘版面、圖片、動畫完全不動。
  全簡報只有投影片 7 出現一次 Reference。

**產出**
- `questions/pytorch.json`（第 3 題）、重建 standalone.html 與 docs/index.html
- `PULSE-Lab0-ANN簡介-30題對照表.pdf`（重新產生）
- `Lab 0 - 神經網路之簡介_2026_v2.pptx`（修正版，80 張，原始檔未動）

**驗證**
- L0-ANN 單元連考四輪 80 題，這次 30/30 種題目全抽到過，選項、正解、判分 0 件不符。
- 修正後的簡報以 python-pptx 重新讀出：投影片 7 為
  「Perception、Memory、Think、Reasoning」，全檔已無 Reference，張數仍為 80。

**問題與修正**
- 無。

**待辦**
- 使用者自行執行 `推上GitHub.bat`。

---

## 2026-09-21　單元改名：L0-ANN → L0-1「Lab0-1 神經網路之簡介」

**需求**
使用者：「Acturally, I want Lab0 and Lab 0-1。So, keep the Lab0 快速上手與環境,
and add Lab 0-1 神經網路之簡解」。也就是沿用題庫既有的 Lab1 / Lab1-2 / Lab1-3 命名慣例。

**做法**
- `unit` 由 `L0-ANN` 改為 `L0-1`，`unit_name` 與 `category` 改為「Lab0-1 神經網路之簡介」。
- id 由 `pt-L0A-xx` 改為 `pt-L0-1-xx`，與 `pt-L1-2-xx`、`pt-L1-3-xx` 一致。
- 位置由「L0 之前」改為「L0 之後」，主持台順序變成 Lab0 → Lab0-1 → Lab1。
- 測試腳本與對照表產生器（`t_l0ann.py`、`mkkey.py`）一併改。

**產出**
- `questions/pytorch.json`（30 題改名重排，題數不變）、重建 standalone.html 與 docs/index.html
- `PULSE-Lab0-1-神經網路之簡介-30題對照表.pdf`（取代原本的 ANN 檔名）

**驗證**
- 單元清單：`L0 Lab0 快速上手與環境（20）` → `L0-1 Lab0-1 神經網路之簡介（30）` → `L1 …`，順序正確。
- L0-1 連考四輪 80 題，30/30 種題目全抽到，選項、正解、判分 0 件不符。
- 主持台建房標題列、單機版單元清單皆正確，無 JS 錯誤；id 無撞號。

**問題與修正**
- 使用者說「沒看到新的 Lab0」，原因是改動只在這個工作環境的 repo 裡，
  還沒推上 GitHub、Render 也還沒重新部署；不是程式問題。

**待辦**
- 使用者自行執行 `推上GitHub.bat`，Render 重新部署後才會看到 Lab0-1。

---

## 2026-09-21　修正：改好的檔案一直沒有進到使用者的電腦

**需求**
使用者：「I did run 推上GitHub.bat. But nothing changes.」

**做法／問題與修正**
- **這是我的疏失。** v6.0 地區題庫、Lab0-1 這幾次的改動全部只 commit 在雲端工作區的
  repo 裡，從來沒有同步回使用者電腦上的 `益智問答開發\quiz_game`。
  他的本機因此沒有任何新檔案，`推上GitHub.bat` 自然推不出東西
  （確認：本機最後一次 commit 停在 9/18 15:46，`origin/main..HEAD` 為 0，
  `grep -c '"L0-1"' questions/pytorch.json` 為 0，版本字樣仍是 v5.9）。
  前幾次我都直接叫他去跑 .bat，等於讓他白跑了好幾趟。
- 修正：用 `git diff --name-only bfbdba1 HEAD` 取出這幾次改動的 26 個檔案，
  以 `device_commit_files` 寫回他電腦上的 quiz_game，全部寫入成功、0 件被拒。
- 在 `CLAUDE.md` 補上規則：雲端 commit 不等於使用者電腦有東西，
  順序必須是「雲端改好 → 測試 → 同步檔案到他的電腦 → 他自己跑 .bat」，
  而且同步後要在他電腦上 `git status` 確認。

**驗證（在使用者電腦上執行）**
- 版本字樣 v6.0、`questions/r_*.json` 七個檔都在。
- 題庫總數 3260 題；地理七區各 55 題。
- Deep Learning 前三個單元：L0 Lab0 快速上手與環境 → L0-1 Lab0-1 神經網路之簡介 → L1。
- `git status` 看得到 26 個檔案的變更與 7 個新題庫檔。

**待辦**
- 使用者執行 `推上GitHub.bat`，這次會真的推得出去；Render 重新部署後首頁應顯示 v6.0。

---

## 2026-09-21　修正：考完後「關閉考坊」按了沒反應

**需求**
使用者：「考完後 關閉考坊 無法運作」。

**問題與修正**
- 重現：主持台走即時連線（SSE）時關閉正常；**一旦退到備援輪詢模式就會卡住**。
  伺服器 `/api/mp/close` 會把房間從 `rooms` 移除，之後 `/api/mp/view` 回 404 + `{"gone":true}`，
  但主持台的輪詢寫成
  `.then(r => r.ok ? r.json() : Promise.reject()).then(handle).catch(() => {})` ——
  404 被 `catch` 整個吞掉，`handle()` 永遠不會收到 `state: "closed"`，
  畫面就一直停在放榜頁，每兩秒撞一次 404。
  Render 或校園網路把 SSE 擋掉／手機當主持台時，走的正是這條路，所以在雲端上才會遇到。
- 修正三處：
  1. 抽出 `leaveRoom()`：收掉連線、清 sessionStorage、回到開考坊畫面，
     推播說已關閉、輪詢發現房間不見了、主持人自己按關閉，三條路共用。
  2. 輪詢改成 `r.status === 404 → leaveRoom()`，不再把錯誤吞掉。
  3. 兩顆「關閉考坊」按鈕改成送出成功就直接 `leaveRoom()`，不等伺服器再推一次狀態
     （備援模式下根本等不到）；放榜頁那顆按下後先 disable，避免連按。
- 學生端 `play.html` 同一個毛病：404 時 `d.gone` 分支進不去，會誤顯示「請重新加入」。
  改成 `r.ok || j.gone` 都當成正常回應，並在關閉時停掉輪詢、顯示「考坊已關閉」。

**驗證**
- 新增 `t_poll_gone.py`：強制主持台走備援輪詢，再從外部關掉考坊。
  **修正前**：主持台卡在原畫面（✗）。**修正後**：回到開考坊畫面（✓）。
- SSE 模式的完整流程（開房 → 五題考完 → 放榜 → 按關閉）：主持台回到開考坊畫面，
  學生看到「考坊已關閉　謝謝參加！」。
- 回歸測試 t_mp / t_clear / t_rejoin / t_zombie / t_autoreveal 全部通過，無 JS 錯誤。

**待辦**
- 同步到使用者電腦後，請他跑 `推上GitHub.bat`。

---

## 2026-09-21　修正：主持台開起來沒有「主題」那一排

**需求**
使用者跑 `啟動-Windows.bat`，主持台畫面上只有題數／每題秒數／賽制與課堂設定，
**主題與單元那兩排整個不見**（附圖）。

**問題與修正**
- 原因：`setup()` 裡是
  `const cats = await fetch('/api/categories')...catch(() => [])` ——
  一旦這支 API 撲空就靜靜給一個空陣列，主題格子就整排消失，而且不會自己重試。
- 為什麼現在才發生：`啟動-Windows.bat` 是
  `start "" http://127.0.0.1:5000/host` **寫在** `%PY% app.py` **前面**，
  瀏覽器先開、伺服器後起。題庫從 2370 題長到 3260 題之後，app.py 載入慢了一兩秒，
  瀏覽器（host.html 可能還是從快取拿的）就比伺服器早一步發出那支請求，撲空。
- 三處修正：
  1. `host.html` 加 `getJSON()`：最多重試 8 次、每次間隔 0.4 秒，並帶 `cache: 'no-store'`；
     真的拿不到就顯示「讀不到主題清單」與「重新載入」按鈕，不再留一片空白。
  2. `index.html` 的 `api.categories` 同樣加上重試，失敗才丟出明確訊息。
  3. `啟動-Windows.bat` 改成先用 PowerShell 輪詢 `/api/categories`，
     確認伺服器真的回應 200 之後才開瀏覽器（最多等 36 秒）。
- 順手清掉 `mp_setup` 裡殘留的 `region`：全域地區早就改用單元了，
  舊值留著會讓每個主題都算成 0 題。

**驗證**
- 新增 `t_slowstart.py`：前 3 秒把 `/api/categories` 全部擋掉（模擬伺服器還沒起來）。
  畫面在 3.8 秒後自己救回來，12 個主題都在，單元清單也正常。
- 伺服器一直起不來的情況：主持台顯示「讀不到主題清單 ＋ 重新載入」，
  單人首頁顯示「連不到伺服器，請確認 app.py 有在執行」。
- 回歸：t_close / t_poll_gone / t_mp / t_clear / t_rejoin / t_autoreveal / t_regionui 全數通過，無 JS 錯誤。

**問題與修正（推不上 GitHub）**
- 使用者「跑了 .bat 但 Render 沒變」。查到 `.git/index.lock` 與
  `.git/objects/maintenance.lock` 卡在那裡 —— **是我先前從掛載層下 `git fetch`
  造成的**（那一層不允許刪檔，git 自己清不掉 lock）。
  index.lock 存在時 `git add -A` 會直接失敗，.bat 因此判定「沒有新的變更需要提交」，
  什麼都沒推。已請使用者授權後刪除這兩個檔。
- 教訓寫進 CLAUDE.md：**不要從掛載層對使用者的 repo 下任何會寫入 .git 的 git 指令**
  （fetch / add / commit / gc 都不行），唯讀查詢（log / status / ls-tree）才安全。

**待辦**
- 使用者跑 `推上GitHub.bat`。

---

## 2026-09-21　關閉考坊前先確認成績收好了

**需求**
使用者：「關閉考坊 按下去後必須先確認成績有下載」。
考坊一關，房間就從記憶體移除，房號、名單、這一場的成績全部消失，拿不回來。

**做法**
- 前端記一個 `gotCsv`：按過「匯出本場成績 CSV」或「分享成績」（分享成功／退回下載都算）
  就設為 true；**新的一局開始（state 為 question 且第 1 題）會重置**，
  所以「再來一局」之後要重新收一次。
- 放榜頁按「關閉考坊」時先盤點還缺什麼：
  ① 還沒匯出 CSV ② 有班級代碼但還沒存入累積積分。
  兩項都齊了 → 照舊跳一次確認就關。
  還缺 → **不關**，在畫面上展開一塊提醒：列出缺哪幾項，並寫明
  「關閉之後這一場的成績就拿不回來了」，附兩顆按鈕：
  「回去收成績」（收起提醒並捲到該按的那顆按鈕）與
  「我已經存好了，直接關閉」（再跳一次確認才真的關）。
- 匯出連結按過之後文字會變成「成績已匯出 ✓　再按可重新下載」，
  重新進放榜頁也維持這個狀態，老師一眼看得出收過沒。

**驗證（新增 t_closeguard.py、t_closeguard2.py）**
- 沒有班級代碼：沒下載就按關閉 → 擋下並提醒「這一場的成績還沒匯出 CSV」，考坊仍在；
  下載後再按 → 跳「成績都收好了。確定要關閉考坊嗎？」→ 關閉成功。
- 有班級代碼：提醒同時列出 CSV 與「還沒存入班級「…」的累積積分」兩項；
  兩項都做完才走一般確認流程。
- 「再來一局」之後再按關閉 → 又提醒一次（新的一局要重新收），符合預期。
- 「我已經存好了，直接關閉」→ 會再跳一次「確定？這一場的成績關掉就找不回來了。」才關。
- t_close.py 依新行為更新（先確認有擋，再走強制關閉）；
  t_poll_gone / t_mp / t_clear / t_rejoin / t_slowstart 全數通過，無 JS 錯誤。

**待辦**
- 「再來一局」也會把上一局的成績洗掉，目前沒有同樣的提醒；要不要加，等使用者決定。
