# 脈衝 PULSE 開發歷程

只追加，不改寫。欄位：需求／做法／產出／驗證／問題與修正／待辦。
本檔自 2026-09-18 起記錄；在此之前的歷程請看 `git log`。

---

## 2026-09-18　v6.0　地理／歷史／人文改成分地區出題

**需求**
使用者：「在地理、歷史、人文等部分，分區域：如中國、台灣、歐洲、美洲、中南美洲等，
規劃一下如何區分比較好，讓 host 可以選擇區域出題，再分別增加區域來出題，
例如：中國地理可加入河北省會是哪裡？中國分多少省？最大的省？等等」，
並要求再做一次 60 人壓力測試。
確認過的選項：分 7 區、每區每主題補到 55 題、主持台「先選主題再選地區」。

**做法**
- 地區不另做一套機制，直接沿用 Python 週次那套 `unit`，主持台與單機版的既有 UI 不必改。
- 七區：TW 台灣／CN 中國／EA 日韓・東亞／SEA 東南亞・南亞／EU 歐洲／AM 美洲／
  OTH 中東・非洲・大洋洲；人文另保留 POEM 古典詩詞。
- `app.py` 新增 `REGION_ORDER`、`REGION_TOPICS`，`units_of()` 依此排序，
  並濾掉題數少於 5 題的單元；`build_standalone.py` 做同樣排序，離線版順序才會一致。
- 新題庫放在 `questions/r_*.json`，一個地區一個檔。
- 出題：21 個（主題 × 地區）各派一個 agent 撰寫並自行查證，共補 860 題。
- 查核：另外派 11 個 agent，拿到的是「只有題目、沒有出處」的檔案，逐題獨立覆核。

**產出**
- 新增 860 題，21 個組合各達 55 題；題庫 2370 → 3230 題。
- `questions/r_taiwan / r_china / r_eastasia / r_southasia / r_europe / r_america / r_other.json`
- 版本 v5.9 → v6.0（`static/*.html`、`DEPLOY-Render.md`、`docs/*.html` 一併更新）
- `docs/課堂小抄.html` 補上「單元／地區」欄位說明

**驗證**
- 21 個（主題 × 地區）各抽三輪、共 1260 題：伺服器發的題目、選項、正解、判分
  與題庫完全一致，0 件不符。
- 60 人同場：加入、送題、搶答、放榜 60/60，0 錯誤，送題平均 15 ms。
- 60 人考到一半中斷 → 換一套題目 → 重新開始：60/60 人在 0.3 秒內跟上。
- 三位老師同時開課 165 人：送題 165/165，0 錯誤，記憶體 66 MB。
- CPU 每題 0.20 秒（換算 Render 免費方案約 2.0 秒）。
- 主持台、單機版、公開版三邊 UI 皆正常，無 JS 錯誤。

**問題與修正**
- 860 題查出 37 個問題，**沒有一題答案錯**，全部是解說用字、譯名一致性或題幹精確度。
  例：「歐洲最大的活火山」是不嚴謹的最高級說法（維基只稱 one of the tallest）；
  「蒙古西部與哈薩克交界」是錯的前提（兩國並不相鄰，最近處相距約 37 公里）；
  巴拿馬運河人工湖那題有兩個選項都說得通（米拉弗洛雷斯湖也是運河上的人工湖）；
  「北条氏」用到日文新字體，應作「北條氏」。全部逐條修正。
- 錫克教創始人一題在 history_SEA 與 human_SEA 重複出現，把歷史那題換成錫克帝國的建立者。
- **失敗的嘗試**：一開始想讓判分測試一次考完整區 55 題，但 `COUNTS` 只允許 5/10/15/20，
  建房直接被擋下來；改成每區跑三輪 20 題，三輪下來每區都能抽到 36–46 種不同題目。
- **失敗的嘗試**：第一版測試腳本用 `res["correct"]` 判斷答對與否，但 `/api/mp/answer`
  回傳的是 `ok`；1260 題全被誤判成「判分不符」。改看 reveal 的 `answer` 與 `result.right`。
- 兩個 agent（human_SEA、human_AM）中途因 session rate limit 或內容過濾被中斷；
  human_AM 的檔案其實已寫完可用，human_SEA 重跑一次，內容過濾那批改成分成兩小批就通過。
- 課堂小抄加了地區說明後超出一張 A4（1013 → 1111 px），把主題列與名單 CSV 列壓成單行後回到 1039 px。

**待辦**
- 使用者自行執行 `推上GitHub.bat` 推上 GitHub，Render 重新部署後首頁應顯示 v6.0。
- GLOBAL（全球・跨區）地理僅 6 題、歷史與人文各 3 題（歷史、人文的已被自動隱藏），
  日後若要保留這個地區要再補題。

---

## 2026-09-21　Deep Learning 新增 L0-ANN 單元（類神經網路之簡介 30 題）

**需求**
使用者上傳《Lab 0 - 神經網路之簡介_2026_v1.pptx》（80 張投影片），要求
「add L0 - ANN簡介 problem bank into this game，30 problems」，隔天上課要用。
確認過的選項：新開一個單元（不動原本的 L0 環境題）、難度照慣例 25/50/25。

**做法**
- 先用 python-pptx 把 80 張投影片的文字與表格全部抽出來（含備註），作為唯一出處。
- 新單元 `L0-ANN`、`unit_name` 為「Lab0 神經網路簡介」，id 前綴 `pt-L0A-`，
  插在 `questions/pytorch.json` 最前面，主持台清單上會排在「Lab0 快速上手與環境」之前。
- 30 題平均分布在簡報的 Part 1–10：基本概念 3、發展歷史 8、分類 3、學習法則 2、
  Perceptron 4、Backpropagation 4、現代架構 4、傳統 vs 深度 1、現代挑戰 1。
- 寫完後派一個獨立查核 agent，拿題目與簡報全文逐題比對，並對外部公開資料查證。

**產出**
- `questions/pytorch.json` 708 → 738 題；題庫總數 3230 → 3260 題。
- `/mnt/user-data/outputs/PULSE-Lab0-ANN簡介-30題對照表.pdf`（列印用，綠色為正解，附解說）

**驗證**
- 結構：30 題、難度 8/15/7、正解索引 8/8/7/7、「最長的選項就是答案」13%（以顯示寬度計）。
- 判分：`L0-ANN` 單元連考四輪共 80 題，抽到 29/30 種不同題目，選項、正解、判分 0 件不符。
- UI：主持台 Deep Learning 的單元清單第一個即為「L0-ANN Lab0 神經網路簡介（30）」，
  建房標題列正確；單機版重建後同樣正確；無 JS 錯誤。
- 查核 agent 順手驗算了簡報自己的兩組手算，數字都自洽：
  AND 閘（W=[1,2]、b=0、η=1）確實在第 1 個 epoch 兩次修正後得 W=[1,1]、b=−2；
  2-2-1 BP 範例的誤差由 0.028473 降到 0.026776（−5.96%）算得出來。

**問題與修正**
- 查核查出 8 題有問題，**沒有一題答案錯**，都是解說或題幹，已全部修正：
  「Neurons that fire together, wire together」不是 Hebb 原文，是 Carla Shatz（1992）的改寫；
  AlexNet 官方 top-5 錯誤率是 15.3%（簡報寫 16%），解說補註實際數字；
  「隱藏層梯度小 100 倍」不能全歸因於 Sigmoid，有一半來自輸入值本身很小（x = 0.05、0.10）；
  BP 並非 1986 年首創（Linnainmaa 1970、Werbos 1974），1986 是普及年；
  第二次寒冬 1995–2006 是就類神經網路而言（學界通稱的第二次 AI 寒冬是 1987–1993），題幹加了限定；
  ResNet 那題的誘答「3.6%」與題幹已給的 3.57% 幾乎同值，換成 1.2%；
  pt-L0A-04 與 05 的解說都自稱「起點」，改成區分「第一個 ANN」與「第一個可學習的 ANN」。
- **沒有照單全收的一項**：查核建議把簡報投影片 7 的「Reference」改成「Reasoning」
  （認為是誤植）。這是老師自己的教材用語，不應由我們逕行更改，
  因此選項保留原文 Reference、只拿掉原本錯誤的中譯「推論」，另外向使用者提出。
- **失敗的嘗試**：第一版先寫好題目再用程式重排選項來平衡正解索引，
  結果「最長的選項就是答案」高達 43% —— 因為正解常常是完整敘述、誘答卻很短，
  單靠重排解決不了。改成把誘答逐一改寫到和正解差不多長，才降到 13%。
  另外長度改用「中文字算 1、英數算 0.5」的顯示寬度計算，比單純算字元數接近螢幕實況。
- 第一版寫了 32 題，砍掉兩題（突觸神經傳導物質、學習訊號 r 的定義，後者與另外兩題重複）
  並把「BP 依據鏈鎖律」調為簡單題，才湊成 30 題且難度剛好 8/15/7。

**待辦**
- 請使用者確認簡報投影片 7 的「Reference」是否為「Reasoning」之誤植；若要改，題目也要跟著改。
- 使用者自行執行 `推上GitHub.bat`。

---

## 2026-09-21　L0-ANN 第 3 題改用 Reasoning

**需求**
承上則待辦。使用者確認簡報投影片 7 的「Reference」應為「Reasoning」，指示「改成 Reasoning」。

**做法**
- `pt-L0A-03` 的選項與解說把 Reference 改成「推理 Reasoning」。
- 簡報本身也一起改，但依簡報最後一張的提醒**另存新檔**，不覆蓋原始檔：
  用 python-pptx 複製一份後只改該處的 run，其餘版面、圖片、動畫完全不動。
  全簡報只有投影片 7 出現一次 Reference。

**產出**
- `questions/pytorch.json`（第 3 題）、重建 standalone.html 與 docs/index.html
- `PULSE-Lab0-ANN簡介-30題對照表.pdf`（重新產生）
- `Lab 0 - 神經網路之簡介_2026_v2.pptx`（修正版，80 張，原始檔未動）

**驗證**
- L0-ANN 單元連考四輪 80 題，這次 30/30 種題目全抽到過，選項、正解、判分 0 件不符。
- 修正後的簡報以 python-pptx 重新讀出：投影片 7 為
  「Perception、Memory、Think、Reasoning」，全檔已無 Reference，張數仍為 80。

**問題與修正**
- 無。

**待辦**
- 使用者自行執行 `推上GitHub.bat`。

---

## 2026-09-21　單元改名：L0-ANN → L0-1「Lab0-1 神經網路之簡介」

**需求**
使用者：「Acturally, I want Lab0 and Lab 0-1。So, keep the Lab0 快速上手與環境,
and add Lab 0-1 神經網路之簡解」。也就是沿用題庫既有的 Lab1 / Lab1-2 / Lab1-3 命名慣例。

**做法**
- `unit` 由 `L0-ANN` 改為 `L0-1`，`unit_name` 與 `category` 改為「Lab0-1 神經網路之簡介」。
- id 由 `pt-L0A-xx` 改為 `pt-L0-1-xx`，與 `pt-L1-2-xx`、`pt-L1-3-xx` 一致。
- 位置由「L0 之前」改為「L0 之後」，主持台順序變成 Lab0 → Lab0-1 → Lab1。
- 測試腳本與對照表產生器（`t_l0ann.py`、`mkkey.py`）一併改。

**產出**
- `questions/pytorch.json`（30 題改名重排，題數不變）、重建 standalone.html 與 docs/index.html
- `PULSE-Lab0-1-神經網路之簡介-30題對照表.pdf`（取代原本的 ANN 檔名）

**驗證**
- 單元清單：`L0 Lab0 快速上手與環境（20）` → `L0-1 Lab0-1 神經網路之簡介（30）` → `L1 …`，順序正確。
- L0-1 連考四輪 80 題，30/30 種題目全抽到，選項、正解、判分 0 件不符。
- 主持台建房標題列、單機版單元清單皆正確，無 JS 錯誤；id 無撞號。

**問題與修正**
- 使用者說「沒看到新的 Lab0」，原因是改動只在這個工作環境的 repo 裡，
  還沒推上 GitHub、Render 也還沒重新部署；不是程式問題。

**待辦**
- 使用者自行執行 `推上GitHub.bat`，Render 重新部署後才會看到 Lab0-1。

---

## 2026-09-21　修正：改好的檔案一直沒有進到使用者的電腦

**需求**
使用者：「I did run 推上GitHub.bat. But nothing changes.」

**做法／問題與修正**
- **這是我的疏失。** v6.0 地區題庫、Lab0-1 這幾次的改動全部只 commit 在雲端工作區的
  repo 裡，從來沒有同步回使用者電腦上的 `益智問答開發\quiz_game`。
  他的本機因此沒有任何新檔案，`推上GitHub.bat` 自然推不出東西
  （確認：本機最後一次 commit 停在 9/18 15:46，`origin/main..HEAD` 為 0，
  `grep -c '"L0-1"' questions/pytorch.json` 為 0，版本字樣仍是 v5.9）。
  前幾次我都直接叫他去跑 .bat，等於讓他白跑了好幾趟。
- 修正：用 `git diff --name-only bfbdba1 HEAD` 取出這幾次改動的 26 個檔案，
  以 `device_commit_files` 寫回他電腦上的 quiz_game，全部寫入成功、0 件被拒。
- 在 `CLAUDE.md` 補上規則：雲端 commit 不等於使用者電腦有東西，
  順序必須是「雲端改好 → 測試 → 同步檔案到他的電腦 → 他自己跑 .bat」，
  而且同步後要在他電腦上 `git status` 確認。

**驗證（在使用者電腦上執行）**
- 版本字樣 v6.0、`questions/r_*.json` 七個檔都在。
- 題庫總數 3260 題；地理七區各 55 題。
- Deep Learning 前三個單元：L0 Lab0 快速上手與環境 → L0-1 Lab0-1 神經網路之簡介 → L1。
- `git status` 看得到 26 個檔案的變更與 7 個新題庫檔。

**待辦**
- 使用者執行 `推上GitHub.bat`，這次會真的推得出去；Render 重新部署後首頁應顯示 v6.0。

---

## 2026-09-21　修正：考完後「關閉考坊」按了沒反應

**需求**
使用者：「考完後 關閉考坊 無法運作」。

**問題與修正**
- 重現：主持台走即時連線（SSE）時關閉正常；**一旦退到備援輪詢模式就會卡住**。
  伺服器 `/api/mp/close` 會把房間從 `rooms` 移除，之後 `/api/mp/view` 回 404 + `{"gone":true}`，
  但主持台的輪詢寫成
  `.then(r => r.ok ? r.json() : Promise.reject()).then(handle).catch(() => {})` ——
  404 被 `catch` 整個吞掉，`handle()` 永遠不會收到 `state: "closed"`，
  畫面就一直停在放榜頁，每兩秒撞一次 404。
  Render 或校園網路把 SSE 擋掉／手機當主持台時，走的正是這條路，所以在雲端上才會遇到。
- 修正三處：
  1. 抽出 `leaveRoom()`：收掉連線、清 sessionStorage、回到開考坊畫面，
     推播說已關閉、輪詢發現房間不見了、主持人自己按關閉，三條路共用。
  2. 輪詢改成 `r.status === 404 → leaveRoom()`，不再把錯誤吞掉。
  3. 兩顆「關閉考坊」按鈕改成送出成功就直接 `leaveRoom()`，不等伺服器再推一次狀態
     （備援模式下根本等不到）；放榜頁那顆按下後先 disable，避免連按。
- 學生端 `play.html` 同一個毛病：404 時 `d.gone` 分支進不去，會誤顯示「請重新加入」。
  改成 `r.ok || j.gone` 都當成正常回應，並在關閉時停掉輪詢、顯示「考坊已關閉」。

**驗證**
- 新增 `t_poll_gone.py`：強制主持台走備援輪詢，再從外部關掉考坊。
  **修正前**：主持台卡在原畫面（✗）。**修正後**：回到開考坊畫面（✓）。
- SSE 模式的完整流程（開房 → 五題考完 → 放榜 → 按關閉）：主持台回到開考坊畫面，
  學生看到「考坊已關閉　謝謝參加！」。
- 回歸測試 t_mp / t_clear / t_rejoin / t_zombie / t_autoreveal 全部通過，無 JS 錯誤。

**待辦**
- 同步到使用者電腦後，請他跑 `推上GitHub.bat`。

---

## 2026-09-21　修正：主持台開起來沒有「主題」那一排

**需求**
使用者跑 `啟動-Windows.bat`，主持台畫面上只有題數／每題秒數／賽制與課堂設定，
**主題與單元那兩排整個不見**（附圖）。

**問題與修正**
- 原因：`setup()` 裡是
  `const cats = await fetch('/api/categories')...catch(() => [])` ——
  一旦這支 API 撲空就靜靜給一個空陣列，主題格子就整排消失，而且不會自己重試。
- 為什麼現在才發生：`啟動-Windows.bat` 是
  `start "" http://127.0.0.1:5000/host` **寫在** `%PY% app.py` **前面**，
  瀏覽器先開、伺服器後起。題庫從 2370 題長到 3260 題之後，app.py 載入慢了一兩秒，
  瀏覽器（host.html 可能還是從快取拿的）就比伺服器早一步發出那支請求，撲空。
- 三處修正：
  1. `host.html` 加 `getJSON()`：最多重試 8 次、每次間隔 0.4 秒，並帶 `cache: 'no-store'`；
     真的拿不到就顯示「讀不到主題清單」與「重新載入」按鈕，不再留一片空白。
  2. `index.html` 的 `api.categories` 同樣加上重試，失敗才丟出明確訊息。
  3. `啟動-Windows.bat` 改成先用 PowerShell 輪詢 `/api/categories`，
     確認伺服器真的回應 200 之後才開瀏覽器（最多等 36 秒）。
- 順手清掉 `mp_setup` 裡殘留的 `region`：全域地區早就改用單元了，
  舊值留著會讓每個主題都算成 0 題。

**驗證**
- 新增 `t_slowstart.py`：前 3 秒把 `/api/categories` 全部擋掉（模擬伺服器還沒起來）。
  畫面在 3.8 秒後自己救回來，12 個主題都在，單元清單也正常。
- 伺服器一直起不來的情況：主持台顯示「讀不到主題清單 ＋ 重新載入」，
  單人首頁顯示「連不到伺服器，請確認 app.py 有在執行」。
- 回歸：t_close / t_poll_gone / t_mp / t_clear / t_rejoin / t_autoreveal / t_regionui 全數通過，無 JS 錯誤。

**問題與修正（推不上 GitHub）**
- 使用者「跑了 .bat 但 Render 沒變」。查到 `.git/index.lock` 與
  `.git/objects/maintenance.lock` 卡在那裡 —— **是我先前從掛載層下 `git fetch`
  造成的**（那一層不允許刪檔，git 自己清不掉 lock）。
  index.lock 存在時 `git add -A` 會直接失敗，.bat 因此判定「沒有新的變更需要提交」，
  什麼都沒推。已請使用者授權後刪除這兩個檔。
- 教訓寫進 CLAUDE.md：**不要從掛載層對使用者的 repo 下任何會寫入 .git 的 git 指令**
  （fetch / add / commit / gc 都不行），唯讀查詢（log / status / ls-tree）才安全。

**待辦**
- 使用者跑 `推上GitHub.bat`。

---

## 2026-09-21　關閉考坊前先確認成績收好了

**需求**
使用者：「關閉考坊 按下去後必須先確認成績有下載」。
考坊一關，房間就從記憶體移除，房號、名單、這一場的成績全部消失，拿不回來。

**做法**
- 前端記一個 `gotCsv`：按過「匯出本場成績 CSV」或「分享成績」（分享成功／退回下載都算）
  就設為 true；**新的一局開始（state 為 question 且第 1 題）會重置**，
  所以「再來一局」之後要重新收一次。
- 放榜頁按「關閉考坊」時先盤點還缺什麼：
  ① 還沒匯出 CSV ② 有班級代碼但還沒存入累積積分。
  兩項都齊了 → 照舊跳一次確認就關。
  還缺 → **不關**，在畫面上展開一塊提醒：列出缺哪幾項，並寫明
  「關閉之後這一場的成績就拿不回來了」，附兩顆按鈕：
  「回去收成績」（收起提醒並捲到該按的那顆按鈕）與
  「我已經存好了，直接關閉」（再跳一次確認才真的關）。
- 匯出連結按過之後文字會變成「成績已匯出 ✓　再按可重新下載」，
  重新進放榜頁也維持這個狀態，老師一眼看得出收過沒。

**驗證（新增 t_closeguard.py、t_closeguard2.py）**
- 沒有班級代碼：沒下載就按關閉 → 擋下並提醒「這一場的成績還沒匯出 CSV」，考坊仍在；
  下載後再按 → 跳「成績都收好了。確定要關閉考坊嗎？」→ 關閉成功。
- 有班級代碼：提醒同時列出 CSV 與「還沒存入班級「…」的累積積分」兩項；
  兩項都做完才走一般確認流程。
- 「再來一局」之後再按關閉 → 又提醒一次（新的一局要重新收），符合預期。
- 「我已經存好了，直接關閉」→ 會再跳一次「確定？這一場的成績關掉就找不回來了。」才關。
- t_close.py 依新行為更新（先確認有擋，再走強制關閉）；
  t_poll_gone / t_mp / t_clear / t_rejoin / t_slowstart 全數通過，無 JS 錯誤。

**待辦**
- 「再來一局」也會把上一局的成績洗掉，目前沒有同樣的提醒；要不要加，等使用者決定。

---

## 2026-09-22　「再來一局」也要先確認成績收好了

**需求**
使用者：「再來一局 按下時先確認是 csv 是否有儲存，沒有再提醒」。
（承上一則的待辦：再來一局會把上一局的分數歸零、題目重抽，成績同樣會消失。）

**做法**
- 把上一則為「關閉考坊」寫的檢查抽成放榜頁內的共用函式 `guard(w)`，
  兩顆按鈕共用同一塊提醒區與同一套判斷（未匯出 CSV／有班級代碼卻未存入累積積分），
  只有文案與要執行的動作不同：
  - 關閉考坊：「先把成績收好，再關考坊」／「關閉之後這一場的成績就拿不回來了，
    考坊一關，房號與名單都會消失」／「我已經存好了，直接關閉」
  - 再來一局：「先把成績收好，再開下一局」／「再來一局會把這一場的分數歸零、題目重抽，
    這一份成績就被蓋掉了（房號與名單會留著）」／「我已經存好了，開下一局」
- 兩者都齊了才跳一次確認（文案分別是「確定要關閉考坊嗎？」與「要開下一局嗎？」），
  硬要做也要再確認一次。

**驗證（新增 t_againguard.py）**
- 沒收成績就按「再來一局」→ 擋下並提醒，仍停在放榜頁 ✓
- 「回去收成績」→ 提醒收起、捲到該按的按鈕 ✓
- 匯出後再按 → 跳「成績都收好了。要開下一局嗎？」→ 順利進入下一局 ✓
- 第二局考完再按 → 又提醒一次（新的一局要重新收）✓
- 回歸：t_closeguard / t_closeguard2 / t_close / t_poll_gone / t_mp / t_clear /
  t_rejoin / t_slowstart 全數通過，無 JS 錯誤。
  （t_closeguard2、t_close 裡的按鈕 id 由 `#closeanyway` 改為共用的 `#goanyway`，已同步更新。）

**待辦**
- 使用者跑 `推上GitHub.bat`。

---

## 2026-09-22　v6.1　先開好「AI 導論」與「EMBA」兩個主題的位置

**需求**
使用者：AI 導論排第三；另外新增 EMBA，底下分五個科目（機器視覺檢測技術、AOI 案例分享、
AI 簡介、Agentic AI、AI War），排第五。**先開好 space，教材之後再給。**
（AI 導論的十六週題目原本要這一輪做，但抓投影片時使用者電腦的橋接斷線，改為先開位置。）

**做法**
- `TOPICS` 插入 `ai`（AI 導論・印記「導」）與 `emba`（EMBA・印記「商」），
  順序變成 Python → Deep Learning → **AI 導論** → VBA → **EMBA** → 地理…
- `FILES` 加上 `ai`、`emba` 兩個題庫檔，先各放一個空陣列。
- 新增 `UNIT_ORDER` 與 `UNIT_NAMES`：把課程的單元順序與中文名稱先寫死，
  `units_of()` 依此排序（和地區主題用 `REGION_ORDER` 是同一個作法）。
  - `ai`：W1–W16 ＋ `MIX`（綜合題組・跨週整合）
  - `emba`：`MV` 機器視覺檢測技術／`AOI` AOI 案例分享／`AIINTRO` AI 簡介／
    `AGENTIC` Agentic AI／`AIWAR` AI War
- 主題格子：題數為 0 時顯示「**準備中**」而不是「0 題」（原本就會自動變灰、點不下去）。
- `style.css` 給兩個主題各一套配色：AI 導論是電路藍＋訊號橙（和 Deep Learning 的紫分得開），
  EMBA 是墨綠＋金。背景底紋等題目進來再補。
- `build_standalone.py` 的 `PRIVATE` 加入 `ai`、`emba` —— 課程教材不放到公開版。

**驗證**
- 主持台與單人首頁的主題順序：Python → Deep Learning → AI 導論（準備中，點不下去）
  → VBA → EMBA（準備中，點不下去）→ 地理…，位置與狀態都正確，無 JS 錯誤。
- 單元排序用假題目驗過：`ai.json` 故意亂序寫入 W10、MIX、W2、W1、W3，
  `units_of('ai')` 回傳 **W1 → W2 → W10 → MIX**（W3 只有 3 題、低於一場最少 5 題，正確地沒出現）；
  `emba` 同樣照 MV → AOI → AIWAR 排好。驗完已把兩個檔案清回空陣列。
- 公開版重建後確認只含地理／歷史／人文／科技／娛樂／趣聞／桌球／綜合，沒有課程主題。
- 回歸：t_mp / t_close / t_closeguard / t_slowstart 全數通過。
- 版本 v6.0 → v6.1。

**待辦**
- AI 導論：十六週各 30 題 ＋ 綜合題組 30 題（共 510 題）。教材在
  `D:\課程\AI and AI Agent\AI Introduction`，以 `_v2` 投影片為準（W04 用 `_v3`），
  可參考 `Video` 裡的字幕；使用者已同意題目可略微超出教材範圍。**等橋接恢復後再抓。**
- EMBA：五個科目的題目，等使用者提供教材。
- 使用者跑 `推上GitHub.bat`。

---

## 2026-09-22　EMBA 新增兩個科目：美中競賽、談台灣現狀與台灣病

**需求**
使用者先後上傳兩份簡報，各要一個科目的競賽題：
《台灣病：除了台積電，台灣還剩下什麼？》（42 頁）與《中美 AI／經濟戰》（26 頁）。

**做法**
- `UNIT_ORDER['emba']` 補上 `USCN`（美中競賽）與 `TWNOW`（談台灣現狀與台灣病），
  接在原本五個科目之後；`UNIT_NAMES` 同步加上中文名稱。
- 各寫 30 題（難度 8/15/7），全部以簡報內容為依據，寫進 `questions/emba.json`。
- 政治敏感題材的處理：凡是作者的論點而非可查證事實，題幹一律寫明
  「**簡報認為／簡報的看法／簡報的分析**」，不把有爭議的立場寫成唯一正解。
- **刻意不出的題材**：台灣現狀那份簡報的「背景 II — 地緣政治」數頁（2026 年委內瑞拉、
  伊朗、荷姆茲海峽等），事件太新、爭議大且無法獨立查證，整段跳過沒有出題。
- 選項長度用「中文算 1、英數算 0.5」的顯示寬度衡量，並加上 2 個字的容差
  （只差一兩個字看不出來，不該算成線索）；正解索引以排列搜尋配平。

**驗證**
- 結構：兩份都是 30 題、難度 8/15/7、正解索引 8/8/7/7；
  「最長就是答案」台灣現狀 3%、美中競賽 20%（門檻 30%）。
- 判分：兩個單元各連考四輪共 80 題，30/30 種題目全抽到，選項、正解、判分 0 件不符。
- UI：EMBA 已由「準備中」變成 60 題可選，科目清單為
  「美中競賽（30）→ 談台灣現狀與台灣病（30）」，建房標題列正確，無 JS 錯誤。
- 另產出兩份列印用對照表 PDF。

**問題與修正**
- 台灣現狀 30 題查出 6 個問題（全數修正）：
  股市市值 5.15 兆美元與公開資料量級不符（解說補註是影片畫面讀值）；
  出口品項選項漏掉簡報名單中的「金屬製品」；
  社工缺額率那題的誘答「台中市 18.4%」是簡報沒有的杜撰數字，已換掉，
  題幹也從「六都」改回簡報實際涵蓋的三都；
  「失智移工」被擴寫成「長照相關移工」會誤導量級，改回簡報原用詞；
  職場霸凌那頁簡報自己寫了「12 倍」又寫「成長 10 倍」，題幹改問「約是幾倍」並在解說揭露。
- 美中競賽 30 題查出 12 個問題（全數修正），其中**有幾項是簡報本身的錯誤，建議老師修投影片**：
  - 投影片 14：H200 功耗寫「Up to 1,000W」→ NVIDIA 官方為 700W（NVL 版 600W），
    1,000W 級距是 Blackwell。
  - 投影片 14：「2025/05 禁運 H200、開放 H20」時序與方向皆不符公開紀錄
    （H100/H200 自 2023/10 起即禁輸中國；2025/05 的指引是針對華為昇騰；
    H20 反而在 2025/04 被加上出口許可要求，7–8 月才恢復）。該題已改成考「簡報認為的技術理由」。
  - 投影片 19：$5.6M 是 **DeepSeek-V3** 最終一次預訓練的 GPU 租用成本，不是 R1。
  - 投影片 4：「1990 年日本人均 GDP 反超美國 21%」→ 世銀資料約 10%（1995 年 +54% 正確）。
  - 投影片 6：任正非《人民日報》專訪是 2025/6/10 不是 5/10；GB200 誤植為「GP200」。
  - 投影片 15：稀土附註列了 15 個鑭系元素卻寫成「7 種」。
  - 投影片 11：把「雲深處科技」標成 Unitree（Unitree 是宇樹科技，雲深處是 DEEPRobotics），
    與投影片 20 自相矛盾。
  - 投影片 21：標題「杭州六小虎」有誤，六小虎全在北京／上海。

**待辦**
- AI 導論十六週題目（教材在使用者電腦，橋接恢復後再抓）。
- EMBA 其餘三科（機器視覺檢測技術、AOI 案例分享、AI 簡介、Agentic AI、AI War）等教材。
- 使用者跑 `推上GitHub.bat`。

---

## 2026-09-22　EMBA・機器視覺檢測技術：修正投影片錯誤並出 30 題

**需求**
使用者上傳四份機器視覺教材，要求：(1) 改掉投影片上發現的錯誤 (2) 出題。
四份為：機器視覺 首頁（6 張）、如何應用機器視覺（4 張）、
Applications of Machine Vision 美國案例（37 張）、四大領域（12 張）。

**做法與產出：投影片修正（一律另存 _v2，原始檔不動）**
- **美國案例**：標題寫「19 Industrial Problems」，條列清單只有 18 項，
  實際逐頁編號的案例卻只有 16 個 —— 三個數字互相打架。
  已把標題與目錄頁統一為 16，並把投影片 4 的清單改成與實際案例頁一致的 16 項
  （原清單有 Glass Bottom Inspector、Dimension Inspection、Door Stamping–Ford、
  Car Body Dimension、PCB–IBM 五項沒有對應頁；實際有的 Cigarette Packaging、
  Rivet Inspection、Speedometer Inspection 反而沒列）。另 Displayer → Display。
- **首頁**：「Q & D」→「Q & A」；內容規劃的「19 Case study」→「16 Case study」。
- **如何應用機器視覺**：「AOI vender」→「AOI vendor」。
- **四大領域**：「Free Chip」→「Flip Chip 覆晶」（半導體封裝沒有 Free Chip 這個詞）。

**沒有動、但建議老師自己確認的幾處**（沒把握，不宜代為改寫）
- 四大領域投影片 12「汽車工業：**陶軸**裁切定位」—— 用詞不明，疑為誤植。
- 同頁「BGA（Ball Grid Array **球閘陣列**封裝）」—— 台灣較常見的譯法是球柵陣列／球格陣列。
- 投影片 6 熱影像「**棕色**代表較高溫」—— 視色階而定，多數熱像儀以白／紅為高溫。
- 如何應用機器視覺投影片 2「搜集製程資訊: **Part rate**」—— 疑為 Pass rate 或 Parts rate。
- 投影片 4「Present/Absent: **是否呈現**」—— 建議改為「有無檢測」比較貼近業界用語。
- 四份的日期分別是 2015/6/15、2019/7/18、2019/9/3、2026/7/10，同一套課程不一致。

**做法與產出：題目**
- `UNIT_ORDER['emba']` 的 `MV` 補上 30 題，排在科目清單第一位。
- 22 題直接取自四份教材；**8 題（emba-MV-20～27：AOI 全稱、背光／同軸光／暗視野、
  遠心鏡頭、解析度換算、高速取像、漏檢 vs 過殺）在這四份裡沒有出處**，
  是依課程架構中「基礎／元件／打光」三個單元（投影片未提供）加上業界通識寫的，
  題幹都沒有謊稱「教材說」。若老師的那三份投影片講法不同，這 8 題要調整。

**驗證**
- 結構：30 題、難度 8/15/7、正解索引 8/8/7/7、「最長就是答案」7%。
- 判分：MV 單元連考四輪共 80 題，30/30 種題目全抽到，選項、正解、判分 0 件不符。
- 獨立查核 30 題只查出 2 個問題，已修正：
  MV-16「案例共幾個」原本與教材白紙黑字的 19 牴觸（題幹已改為「逐頁說明的 Problem 1～16」，
  解說並註明原稿 19／18／16 不一致、修正版已統一）；MV-02 解說把 OCR 歸入 Decoding 時
  誤稱「教材說」，已改寫。查核另逐條驗證了 8 題無出處題的技術正確性
  （Keyence／Opto Engineering 打光指南、Edmund Optics 遠心鏡頭、AOI 全稱、漏檢過殺用法），
  以及解析度換算 100 mm ÷ 2,000 px = 0.05 mm/px 正確。
- EMBA 題庫 60 → 90 題；全站 3320 → 3350 題。

**待辦**
- AI 導論十六週題目（教材在使用者電腦）。
- EMBA 其餘四科：AOI 案例分享、AI 簡介、Agentic AI、AI War。
- 「基礎／元件／打光」三份投影片若提供，回頭校對 emba-MV-20～27 那 8 題。
- 使用者跑 `推上GitHub.bat`。

---

## 2026-09-22　AI 導論 510 題、EMBA・AOI 案例分享 40 題、機器視覺擴到 50 題

**需求**
使用者陸續交付：(1)「機器視覺檢測技術 增加至 50 題」(2) iVi Lab 案例分享 PDF → AOI 案例分享
(3) AI 導論十六週投影片 → 每週 30 題，可以加一些課外題。

**做法**
- **機器視覺 30 → 50 題**：新增 20 題（線掃描相機、全域/捲簾快門、焦距與視野、光圈與景深、
  穹頂光、二值化、Blob 分析、樣板比對、次像素、形態學、工作距離、相機校正、節拍時間、
  漏檢過殺取捨、現場不穩定來源、深度學習 vs 規則式、只用良品訓練的異常偵測、產業別）。
  50 題的比例調成 13/25/12。
- **AOI 案例分享 40 題**：依 iVi Lab 44 頁、約 25 個案例出題，難度 10/20/10。
- **AI 導論 510 題**：十六週各派一個出題代理（每週至少 22 題有投影片依據、最多 8 題課外並查證），
  再加一組 30 題跨週整合的綜合題組。
- **查核**：AI 分四批（各 120 題）＋綜合題組＋AOI，共六個獨立查核代理；
  查核結果再由四個修正代理逐條套用，並把分布調回規格。

**驗證**
- 結構：每一週／每一組都是剛好 30 題（AOI 40、MV 50），難度 8/15/7、正解索引 8/8/7/7
  （AOI 10/20/10 與 10/10/10/10、MV 13/25/12），「最長就是答案」全部在 3% 以下。
- 判分：AI 導論 17 個單元共測 680 題、EMBA 4 個科目共測 200 題，**選項、正解、判分 0 件不符**。
- UI：主題列 Python → Deep Learning → AI 導論（510）→ VBA → EMBA（150）→ 地理…；
  AI 導論 17 個單元、EMBA 4 個科目都正確列出，無 JS 錯誤。
- 題庫 3370 → **3920 題**。

**問題與修正**
- 480 題查核**沒有一題答案是錯的**，但查出 57 筆品質問題，全部修掉：
  - **重複題**：達特茅斯會議被問了三次、Transformer 論文被問了三次、
    W11-22≡W12-10、W11-23≡W12-11 等，各留一題，其餘改考投影片裡還沒考到的知識點。
  - **相鄰題互相洩答**：ai-W3-24 的誘答就是下一題題幹給的數字；
    ai-W4-19/20 過度配適與欠配適互為鏡像；已改掉誘答或換考點。
  - **ai-W9-02 用了「承上」**——出題會洗牌，前一題不會在場，題幹已改成自足。
  - **會過期的內容**：TAIDE 型號、模型版本、公司股權、超級電腦名次，改成不綁版本的考點；
    OWASP 那題標了版本並加上 `expires`。
  - 用語統一：**迴歸**（不是回歸）、**過度配適**（不是過擬合）、小黃鴨除錯法、阿里巴巴的 Qwen。
- **另外整理出一份《教材待修清單》（50 項）**，都是投影片本身的問題，機器視覺四份已改的以外
  都沒有代為更動。重點包括：W07 的 0.028473 用四捨五入後的數字算不出來、
  W07 隱藏層更新值與 η 不一致、W08「八十年沒有改變」三者不同齡、
  W09 LeNet 1989/1998 與「5 層」的三重混淆、W09 ResNet 並非首次超越人類、
  W11 湧現能力引錯論文（Kaplan 2020 沒談湧現）、W16 超級電腦第 14 名查無依據、
  W15 Optimus Gen 3 尚未發表、Figure 已終止與 OpenAI 合作、
  iVi Lab 有兩張完全重複的投影片、多處「及時」應為「即時」。

**待辦**
- EMBA 其餘三科：AI 簡介、Agentic AI、AI War。
- 使用者跑 `推上GitHub.bat`。

---

## 2026-09-22（補記）同步失敗，改用更新檔 zip 交付

- **需求**：把 AI 導論 510 題、EMBA AOI 40 題、機器視覺 50 題這一批成果送回使用者電腦。
- **做法**：先用 `device_commit_files` 直寫桌面的 `quiz_game\`，失敗（The device this session is bound to is not connected to the bridge）。依工具指引重試一次，仍然連不上，就不再重試。改把 6 個檔案打包成 `PULSE-v6.2-更新檔.zip`，用對話交付。
- **產出**：
  - `PULSE-v6.2-更新檔.zip`：`app.py`、`DEVLOG.md`、`standalone.html`、`questions/ai.json`、`questions/emba.json`、`docs/index.html`（4.2 MB，8 個項目）
  - `PULSE-AI導論-510題對照表.pdf`
  - `PULSE-EMBA-AOI案例分享-40題對照表.pdf`
  - `教材待修清單.pdf`（投影片上發現的 50 處問題）
- **驗證**：解開 zip 對過 8 個項目都在。另外拿每個私有主題前 3 題的題幹去 grep 兩份 build：`docs/index.html` 對 python / pytorch / ai / vba / emba 全部 0 命中，`standalone.html` 全部 3/3 命中，`r_taiwan` 兩邊都 3/3 —— 確認公開版沒有夾帶課程題目。
- **問題與修正**：`docs/index.html` 裡還留著 CSS 註解與一份舊的 `CAT_NAME` 對照（Python / Deep Learning / VBA 的顯示名稱），沒有任何題目內容。不算外洩題庫，但下次整理模板時可以順手拿掉。
- **待辦**：
  1. 使用者解壓縮更新檔覆蓋 `quiz_game\`，再跑 `推上GitHub.bat`，Render 才會更新（自 a2a18f9 之後都還沒推上去）。
  2. EMBA 還缺 AI 簡介、Agentic AI、AI War 三個科目的教材。
  3. 若拿到「基礎／元件／打光」三份機器視覺投影片，要回頭覆核 `emba-MV-20`～`27`（這幾題是照業界慣例寫的，不是照他的投影片）。

---

## 2026-09-23 同步成功；並修掉我自己寫錯的 CLAUDE.md 規則

- **需求**：橋接恢復後，把昨天卡住的檔案真正寫回使用者電腦。
- **做法**：`device_commit_files` 寫入 `questions/ai.json`、`questions/emba.json`、
  `standalone.html`、`DEVLOG.md`（`app.py` 與 `docs/index.html` 先前已同步，md5 已相同）。
- **產出**：使用者電腦上 6 個檔案的 md5 與雲端完全一致；`static/host.html`、`play.html`、
  `index.html`、`style.css`、`build_standalone.py`、`啟動-Windows.bat` 也逐一比對過，全部一致。
- **驗證**：在他電腦上用 Python 讀題庫，`ai=510`、`emba=150`、`python=500`、`pytorch=738`、
  `vba=280`。同步前 `questions/ai.json` 只有 **3 bytes**（`[]`），AI 導論等於是空的——
  這代表昨天那批檔案確實一個都沒到位，不是只有部分沒到。
- **問題與修正**：
  1. 我為了確認檔案狀態，在掛載層跑了 `git status`，又留下一個 `.git/index.lock`。
     這正是 CLAUDE.md 第 47 條自己叫我不要做的事——但**那一條規則本身寫錯了**：
     它把 `status` 列在「唯讀查詢才安全」裡。`git status` 會寫 index.lock 來刷新 index，
     不是唯讀指令。已改掉規則，並把確認方式改成兩邊 `md5sum` 對拉。
  2. 用 `device_request_delete_permission` 要到刪檔權限後 `rm -f .git/index.lock`，
     已確認 `.git/*.lock` 清空，`推上GitHub.bat` 不會再被卡住。
- **待辦**：
  1. 使用者跑 `推上GitHub.bat`，Render 才會更新。
  2. EMBA 還缺 AI 簡介、Agentic AI、AI War 三個科目的教材。
  3. 拿到「基礎／元件／打光」三份機器視覺投影片後，回頭覆核 `emba-MV-20`～`27`。
  4. `docs/index.html` 裡殘留的 CSS 註解與舊 `CAT_NAME` 對照（只有顯示名稱、無題目內容），
     下次整理模板時拿掉。
